import static org.junit.Assert.assertEquals;

import org.junit.Test;

public class BasicTest {
    @Test
    public void testAddition() {
        assertEquals("2 + 2 should equal 4", 2 + 2, 4);
    }
}
